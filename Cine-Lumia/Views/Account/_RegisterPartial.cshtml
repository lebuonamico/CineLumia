@model Cine_Lumia.Models.RegisterViewModel

<section>
    <form asp-action="Register" method="post" data-ajax="true" data-ajax-method="POST" data-ajax-update="#genericModal .modal-body" data-ajax-mode="replace" data-ajax-success="onRegisterSuccess">
        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <div class="mb-3">
            <input asp-for="Nombre" class="form-control" placeholder="Nombre" />
            <span asp-validation-for="Nombre" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <input asp-for="Apellido" class="form-control" placeholder="Apellido" />
            <span asp-validation-for="Apellido" class="text-danger"></span>
        </div>
        
        <div class="mb-3">
            <input asp-for="Email" class="form-control" placeholder="Correo Electrónico" />
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>
        
        <div class="mb-3">
            <input asp-for="Password" class="form-control" placeholder="Contraseña" />
            <span asp-validation-for="Password" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <input asp-for="ConfirmPassword" class="form-control" placeholder="Confirmar Contraseña" />
            <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
        </div>
        
        <div class="d-grid">
            <button type="submit" class="btn btn-primary">Registrarse</button>
        </div>
    </form>
</section>

<script>
    function onRegisterSuccess(data) {
        if (data && data.success) { // Assuming your controller returns { success: true } on successful registration
            // Optionally show a success message
            alert('Registro exitoso. Por favor, inicia sesión.');
            // Close the modal and potentially redirect or refresh
            location.reload(); 
        } else {
            // If registration failed but ModelState was valid, the partial view itself will show errors
            // Re-parse validation scripts for the newly loaded content
            $.validator.unobtrusive.parse('#genericModal .modal-body');
        }
    }
</script>
